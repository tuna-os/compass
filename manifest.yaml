# The last release's tag and the commit it was cut from, written by
# scripts/bump_version.sh (`make bump-patch|minor|major`), which commits this
# file and tags that commit. The build does not read it: the engine's version
# is the workspace's `CARGO_PKG_VERSION`.

release:
  tag: "v0.28.1"
  rev: "e680237b019d13dcd600bd414ced00e28ac14809"
  short_rev: "e680237b0"
