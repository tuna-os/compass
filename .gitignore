.cache
.kilo
/build
/build-*
/.build
.vscode
CMakeUserPresets.json
.tmp
extension-manager/runtime/node_modules
/cmake-build-debug
/CMakeFiles
/.idea
/vgcore*
.env
.direnv

*.tar.gz
!extra/icons/vicinae-icon-theme.tar.gz
/dist

__cmake*
*.tgz

/qt6
**/.qmlls.ini

/AppDir
*AppImage

/result

heaptrack*

.claude/*
!.claude/CLAUDE.md

# you can symlink AGENTS.md yourself
/CLAUDE.md

.omx/
.codex/
.gitnexus/

/tmp

/internal-docs


.worktrees
.envrc.local

# Local translation tooling (stored, not part of translation PRs)
BACKUP/

# Rust
/target

/build-flatpak
/.flatpak-builder
__pycache__/

# Control-harness backups. The mutation harnesses copy a module to <name>.bak
# before editing it and restore from that copy afterwards; nineteen of them
# reached a review branch via `git add -A` before this rule existed.
*.bak

# Generated by `make design-shots` — regenerate in five seconds, never review a diff of them.
tools/design/shots/
